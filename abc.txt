this is push from local repo.
CHECK NEW COMMIT
git pull origin main --allow-unrelated-histories
then 
git push -u origin main.

git diff, git amend.
 =======================
 adding new changes to chech git diff

git diff commit_id_1 coomit_id_2 
